<!DOCTYPE html>
<html>
<head>
	<title> Teams </title>
</head>
<body>
	<h1> Teams: </h1>
	<div>
		<ul>
			<% @teams.each do |team| %>
				<li> <%= link_to team.name, team_path(team) %> </li>
			<% end %>
		</ul>
	</div>

	<!-- DO WE NEED TO CREATE TEAM FROM PAGE TEAMS?-->
	<!-- <div>
		<%= button_to "Create a new Team", new_team_path, method: :get %>
	</div> -->
</body>
</html>